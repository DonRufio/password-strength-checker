# Normalize all text files to use LF in the repository
* text=auto

# Shell scripts should use LF
*.sh text eol=lf

# Batch files should use CRLF
*.bat text eol=crlf

# Ensure Markdown files use LF
*.md text eol=lf

# Treat the following as binary files
*.png binary
*.jpg binary
*.gif binary
*.zip binary
*.ico binary
*.svg binary

# JSON, JS, CSS, HTML, XML should normalize line endings
*.json text
*.js text
*.css text
*.html text
*.xml text
